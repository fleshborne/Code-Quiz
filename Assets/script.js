var startBtnElement = document.querySelector(".start-button");

function startQuiz() {
    // console.log(quizQuestions[0].question);
    // console.log(quizQuestions[0].answer);
    // hide initial screen

    //show the first question => we need a function that's going to getQuestions

    //start the timer - we need a timer function track time

}


startBtnElement.addEventListener("click", startQuiz);
